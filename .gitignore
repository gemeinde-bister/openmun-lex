# Python
__pycache__/
*.pyc
*.py[cod]
*.egg-info/
dist/
build/

# Virtual environments
.venv/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Internal docs (stay in hg)
docs/

# Internal files
SENSITIVE.md
.firecrawl/

# Mercurial (private VCS)
.hg/
.hgignore

# UV lock files (contain internal PyPI URLs)
**/uv.lock

# Law corpora — reproducible via sync scripts, not tracked (matches .hgignore)
data/ch/
data/vs/
data/search_index/
data/bv_101.xml

# Sync run artifacts (regenerated by the sync scripts, not tracked)
data/sync_index.json
data/sync_report.log

# Municipal law data — authored, kept in hg + separate backups; will move to a
# per-municipality data repo (see docs/MUNICIPAL-DATA-PACKAGING.md)
data/mun/

# Proof of concept experiments
pocs/

# Stray manual law PDF download at repo root (not app content)
/611_102.pdf

# OS / editor
.DS_Store
*.swp
*.swo
*~
*.orig
